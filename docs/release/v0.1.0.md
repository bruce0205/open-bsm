# v0.1.0 Release Scope

## Status

Pre-release

## Implemented

- Native Apple Silicon executable。
- InputMethodKit input controller and a custom native candidate bar。
- Plain-text code table loader with normalized, case-insensitive roots。
- Composition of roots up to five characters with inline marked text。
- Exact-match candidate lookup with nine candidates per page。
- Arrow keys move candidate selection；`Page Up` and `Page Down` change pages。
- Mouse-selectable candidates and page controls in one integrated candidate bar。
- `Space` or `Enter` to commit the selected candidate。
- Number keys `1` through `9` to select a candidate from the current page。
- `Backspace` to remove the last root。
- `Escape` to cancel composition。
- `Shift + Space` to switch between Chinese and direct English input，including
  terminal clients such as iTerm2、Ghostty、and the VS Code integrated terminal。
- Punctuation and symbol input exclusively through code-table roots such as
  `.s`、`,s`、`[[`、and `]]`；there are no hard-coded punctuation conversions。
- Personal code table at `~/Library/Application Support/OpenBSM/user.txt`。
- Root-code reverse lookup for selected text with `Option + Shift + R`。
- Input menu item for switching Chinese and English modes。
- User-selectable per-app or global Chinese and English mode memory。
- Global full-width and half-width modes with `Control + Shift + Space`。
- Candidate frequency learning with local，persistent usage history。
- Candidate bar theme switch with dark and light modes。
- Unit tests for table parsing and the input state machine。
- Local build and per-user installation scripts。

## Remaining Before Public Release

- Validate behavior in additional apps such as Safari and Microsoft Office。
- Add an original app icon。
- Add Developer ID signing、notarization、and a distributable installer。
- Add a settings window for shortcuts and table management。

## Known Limitations

- Development builds use ad-hoc signing。
- Public distribution requires Developer ID signing、notarization、and stapling。
- The current scope does not include AI Assistant features；those are tracked in the
  [AI Assistant specification](../specification/ai-assistant/README.md)。

## Release Criteria

The first public release requires completion of all items in
[Remaining Before Public Release](#remaining-before-public-release)，including
validation in additional applications、an original app icon、Developer ID
distribution、and a settings window。

## Release Checklist

### Scope and Versioning

- [ ] Confirm all implemented features are documented in this release scope。
- [ ] Confirm the version is `0.1.0` in `Resources/Info.plist`、README、the release scope、and the user manual。
- [ ] Confirm the Git tag will be `v0.1.0`。
- [ ] Confirm all intended changes are recorded in `CHANGELOG.md` under `[Unreleased]`。

### Quality Assurance

- [ ] Run `swift test` successfully。
- [ ] Run `make build` successfully。
- [ ] Verify the generated `.build/OpenBSM.app` has the expected input-method bundle structure。
- [ ] Run `git diff --check`。
- [ ] Complete code review for the release changes。

### Application Compatibility

- [ ] Validate basic Chinese input in a supported development environment。
- [ ] Validate behavior in Safari。
- [ ] Validate behavior in Microsoft Office。
- [ ] Validate Chinese／English mode switching in terminal clients including iTerm2、Ghostty、and the VS Code integrated terminal。
- [ ] Validate candidate selection、pagination、reverse lookup、personal code table、full-width mode、and candidate frequency learning。

### Distribution

- [ ] Add the original application icon。
- [ ] Build and inspect the development DMG。
- [ ] Build and inspect the macOS Installer package。
- [ ] Sign the release with a Developer ID Application certificate。
- [ ] Sign the installer with a Developer ID Installer certificate。
- [ ] Submit the release artifacts for notarization。
- [ ] Staple and verify notarization results。
- [ ] Confirm installation and removal behavior on a clean test environment。

### Documentation and Publication

- [ ] Confirm the user manual describes the released behavior。
- [ ] Confirm all local Markdown links resolve correctly。
- [ ] Change this document's status to `Released`。
- [ ] Add the official release date to this document。
- [ ] Move the completed `[Unreleased]` entries in `CHANGELOG.md` to `[0.1.0] - YYYY-MM-DD`。
- [ ] Create Git tag `v0.1.0`。
- [ ] Publish and archive the final release artifacts。
